spring:
  profiles:
    active: dev
  datasource:
    name: demo-person
    url: jdbc:mysql://172.28.1.6:3306/demo-person
    username: root
    password: mamahao
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20
server:
  port: 8881
  context-path: /user



eureka:
  instance:
    metadata-map:
      cluster: BC-EBIZ-USER
mybatis:
  mapperLocations: classpath*:mapper/*.xml
  typeAliasesPackage: com.bc.ebiz.user.persist.entity
  configLocation: classpath:/mybatis-config.xml
  checkConfigLocation: true



